(function () {
    'use strict';

    window.foo = function (message) {
        return ('Echo: ' + message);
    };

    function doSomething(someVariable) {
return 'this is a variable ' + someVariable;
    }
} ());
